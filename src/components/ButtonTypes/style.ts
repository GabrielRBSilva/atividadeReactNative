import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  styleButton:{
        width: "100%",
        height: 60,
        borderRadius: 10,
        alignItems: "center",
        justifyContent: "center"
  },

  text: {
    color: "#FFF",
    fontSize: 20,
    fontWeight: "bold",
  },
});